## Packages
xlsx | Export data to Excel
framer-motion | For smooth page transitions and modal animations
lucide-react | For beautiful icons (already in base stack, but confirming usage)
date-fns | For formatting dates nicely

## Notes
WebSocket connection required at /ws for real-time updates.
Authentication uses session cookies (credentials: "include").
Role-based access control is enforced on the backend, frontend should handle redirects/view hiding.
